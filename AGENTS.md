# Repository rules

Before commit, push, or PR change:

- Read `.agents/skills/caveman-pr/SKILL.md`.
- Use it for whole PR.
- Never use `--no-verify`.
- Obey `scripts/check-metadata.mjs`.
- Use Conventional Commits for commit and PR titles.
- PR body needs `Summary`, `Why`, `Validation`.
